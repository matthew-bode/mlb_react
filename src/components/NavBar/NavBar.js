import css from './NavBar.module.css';

const NavBar = () => {
    const test = '';

    return (
        <div className={css.navBarDiv}>
            'NavBar'
        </div>
    )
}

export default NavBar;